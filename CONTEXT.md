# CONTEXT.md — CUMCM Skills 共用语言

本文件给人和 Agent 共用。写代码、写论文、写 skill 时只用这里的词，不要发明同义词。

## 赛事

| 词 | 唯一含义 |
| --- | --- |
| **CUMCM** | Contemporary Undergraduate Mathematical Contest in Modeling。中文官方名：全国大学生数学建模竞赛。不要写成 MCM（那是 COMAP 美赛）。 |
| **国赛** | 仅指 CUMCM，不含美赛、研赛、华为杯。 |
| **赛题** | 当年公布的 A/B/C/D/E 等题目原文 + 附件数据。 |
| **小问** | 赛题中编号的子问题（问题 1、问题 2…）。 |
| **正文** | 电子版摘要页之后、附录之前的内容。官方上限 **30 页**，不要目录。 |
| **摘要专用页** | 电子版第一页。含标题与关键词，**原则上不超过一页**。不要求英文摘要。 |
| **支撑材料** | 与论文分开的 RAR/ZIP（≤20MB）。至少含可运行源程序；使用 AI 时必须含 `AI工具使用详情.pdf`。 |
| **核心建模** | 模型选择、公式推导、机理分析、关键求解逻辑、关键结论。必须由参赛队主导并人工核验。 |

官方规则只认 [mcm.edu.cn](https://www.mcm.edu.cn/) 现行文本。课程讲义、自查表、网上“800–1000 字摘要”“正文 25 页起步”等 **不是** 官方硬性条款；与官方冲突时以官方为准。

## 四大评审轴

国赛评阅长期使用四轴（约分，用于自查权重，不是可诉讼的精确分）：

| 轴 | 约分 | 评委实际看什么 |
| --- | --- | --- |
| 假设合理性 | ~15 | 必要、可辩护、与模型一一对应，通常 3–6 条 |
| 建模创造性 | ~25 | 贴题、有推导、有对比，而不是堆名词 |
| 结果正确性 | ~25 | 答完所有小问、数字可复现、有检验 |
| 文字表述 | ~35 | 摘要、结构、图表、公式、匿名、引用 |

## 题型（互斥优先，其次 hybrid）

| id | 判定 | 不是它 |
| --- | --- | --- |
| `optimize` | 有决策变量 + 目标（最大/最小/最优安排） | 只有“评价哪个更好”而无决策 |
| `evaluate` | ≥2 个对象，要排序/评级 | 只要预测未来数值 |
| `predict` | 用历史/观测推断未观测值 | 用物理定律写微分方程为主 |
| `mechanism` | 用物理/工程/生态机理建方程，数据常用来标定 | 纯拟合黑箱 |
| `stat` | 检验、归因、相关、分布 | 已有明确优化目标 |
| `network` | 图、路径、传播、匹配 | 普通线性规划可表达的分配 |
| `hybrid` | 一小问内必须串联两类；要拆成子步骤 | 图省事把整题标成 hybrid |

## 建模产物

| 词 | 含义 |
| --- | --- |
| **基准模型** | 该题型最稳、能给分的模型。必须先跑通。 |
| **主模型** | 论文采用并详细推导的模型。 |
| **对照模型** | 用来证明主模型不是随便选的。 |
| **创新** | 仅三类算创新：算法改进、跨域迁移、多模型融合。换名字、堆深度学习、无对比，都不算。 |
| **三大检验** | 误差分析、灵敏度分析、稳健性检验。缺一类不能把状态标成 `validated`。 |
| **可复现** | 固定种子、固定路径约定、附录代码能跑出论文数字。 |
| **问题驱动结构** | 正文按小问组织：每问 = 思路 → 建模 → 求解 → 结果。不要按“评价模型大全”组织。 |

## 工作区约定

赛题仓库（不是本 skills 仓库）默认：

```
project/
├── contest-state.json      # 共享状态，所有节点读写
├── problem/                # 赛题原文与附件
├── data/raw/  data/processed/
├── src/  scripts/  configs/
├── results/figures/  results/tables/  results/logs/
├── paper/                  # 论文源与导出
└── support/                # 支撑材料待打包内容
```

Agent 不得把学校、姓名、队号写入 `paper/` 或 `support/`。

## AI 合规（2026 试行）

1. 可以用 AI，不是必须用。
2. 参考文献前必须有「AI 工具使用声明」（用了 / 没用，二选一）。
3. 用了就必须在支撑材料放 `AI工具使用详情.pdf`。
4. 禁止：隐瞒使用、假声明、把未核验的 AI 输出当作核心建模提交。
5. 竞赛期间禁止在队外任何平台讨论赛题（含 GitHub、小红书、CSDN 等）。本技能库用于队内工作区，**不要把未公开赛题推到公开仓库**。

## 写作质量（不是“降 AI 率”）

不要追求检测软件百分比。要写：选择理由、参数来源、失败方案、异常点、边界条件、具体数字。禁止为降重改公式含义或随机改符号。
