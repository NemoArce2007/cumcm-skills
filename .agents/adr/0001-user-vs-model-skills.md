# ADR 0001 — 用户技能编排，模型技能执行

- 状态：已采纳
- 日期：2026-09-10

## 决策

用户点名的技能只负责编排，彼此不互相调用；可以调用模型技能。国赛全流程由 `contest-run` 持有图。单点任务不启动图。

## 理由

读题、求解、写作、合规的验收条件不同。塞进一个超长说明会让 Agent 跳过验收。图把下一跳写死，loop 把停机写死。

## 后果

技能数量变多。用 `ask-cumcm` 做入口，避免用户记全部名字。README 只写本仓库怎么用，不指向其它技能仓库，以免 Agent 读完去调外部技能。
