# ADR 0001 — 用户技能编排，模型技能执行

- 状态：已采纳
- 日期：2026-09-10

## 决策

与 mattpocock/skills 相同：用户调用的 skill 只编排，不互相调用；可调用模型技能。国赛全流程由 `contest-run` 持有图；单点任务不启动图。

## 理由

读题、求解、写作、合规的 verifier 不同。塞进一个超长 prompt 会让 Agent 跳验收。图把路由写死，loop 把停机写死。

## 后果

增加 skill 数量。用 `ask-cumcm` 做入口，避免用户记 17 个名字。
